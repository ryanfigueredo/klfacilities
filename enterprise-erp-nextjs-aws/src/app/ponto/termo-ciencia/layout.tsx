export const dynamic = 'force-dynamic';

// Layout público para /ponto/termo-ciencia - não usa AppShell
export default function TermoCienciaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

